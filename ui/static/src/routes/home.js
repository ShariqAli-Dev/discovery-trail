/** @type {string} */
let shariq = "shariq";
/** @type {string[]} */
const names = ["shariq", "the cool", "was here"];

/**
 * @typedef {Object} User
 * @property {number} id
 * @property {string} name
 */
